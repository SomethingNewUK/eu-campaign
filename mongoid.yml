development:
  clients:
    default:
      database: manifesto_dev
      hosts:
        - localhost:27017

test:
  clients:
    default:
      database: manifesto_test
      hosts:
        - localhost:27017
    
production:
  clients:
    default:
     uri: <%= ENV['MONGODB_URI'] %>
     options:
       connect_timeout: 15